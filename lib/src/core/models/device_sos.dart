import 'package:equatable/equatable.dart';
import 'package:flutter/material.dart';

/// generate by https://javiercbk.github.io/json_to_dart/
class AutogeneratedDeviceSOS {
  final List<DeviceSOS> results;

  AutogeneratedDeviceSOS({this.results});

  factory AutogeneratedDeviceSOS.fromJson(Map<String, dynamic> json) {
    List<DeviceSOS> temp = <DeviceSOS>[];

    if (json['results'] != null) {
      json['results']?.forEach((v) {
        temp.add(DeviceSOS.fromJson(v as Map<String, dynamic>));
      });
    }
    return AutogeneratedDeviceSOS(results: temp);
  }

  Map<String, dynamic> toJson() {
    final data = <String, dynamic>{};
    if (results != null) {
      data['sos'] = results.map((v) => v.toJson()).toList();
    }
    return data;
  }
}

@immutable
class DeviceSOS extends Equatable {
  final int id;
  final String deviceID;
  final String timestamp;
  final String state;

  DeviceSOS({
    @required this.id,
    this.deviceID,
    this.state,
    this.timestamp,
  });

  @override
  List<Object> get props => [id, deviceID];

  factory DeviceSOS.fromJson(Map<String, dynamic> json) {
    return DeviceSOS(
      id: json['id'] as int,
      deviceID: json['deviceID'] as String,
      state: json['state'] as String,
      timestamp: json['timestamp'] as String,
    );
  }

  Map<String, dynamic> toJson() {
    final data = <String, dynamic>{};
    data['id'] = id;
    data['deviceID'] = deviceID;
    data['state'] = state;
    data['timestamp'] = timestamp;
    return data;
  }
}
